# Code Deletion Log

> Location note: this log lives at the repo root, NOT `docs/`, because `docs/` is
> bot-generated (regenerated hourly by `update-models.yml`) and hand-edited files
> there get overwritten. Root is the stable home for hand-maintained records.

## [2026-08-17] Dead code & consolidation sweep

### Unused Dependencies Removed
- None. `pyproject.toml` already has an empty `dependencies` list; dev extras
  (`pytest`, `ruff`) are both used by CI and the dev workflow.

### Unused Files Deleted
- None in this session. (Proxy-era files — `config.yaml`, `docker-compose*.yml`,
  `sync_models.py`, `tests/test_gateway.py`, `scripts/{create-agent-keys,preflight,security-check,smoke-test}.sh`,
  etc. — were already staged for deletion by the main branch work; verified no
  workflow or source file references them.)

### Duplicate Code Consolidated
- `probe_models.py` → `build_request()`: merged the `openai` and `cohere_v2`
  style branches into a single `if style in ("openai", "cohere_v2"):` branch.
  - Reason: both branches produced byte-identical request payloads (same URL
    pattern, same chat-completions-shaped body).
  - Behavior preserved: identical `url`/`body` for both styles; `gemini`,
    `cloudflare` branches untouched.

### Unused Exports Removed
- None. `common.py` (`SafeRedirectHandler`, `_opener`, `_is_free`) is fully live
  — `generate_site.py` imports and uses `_is_free` and `_opener`; every function
  in `probe_models.py` has an internal caller or test coverage.

### Dead Provider Probes Removed (`scripts/provider-check.sh`)
- Removed probe lines for Perplexity (`$PERPLEXITY_API_KEY`), Codestral
  (`$CODESTRAL_API_KEY`), PublicAI (`$PUBLICAI_API_KEY`).
  - Reason: providers are absent from `PROVIDER_PROBES` in `probe_models.py`,
    absent from `.env.example`, and their env vars were undocumented. They were
    leftovers from the LiteLLM proxy era and could never pass
    `test_env_example_covers_dashboard_env_vars`-style consistency.
- Removed stale "Aion Model Gateway" branding (header comment, result footer,
  `/tmp/aion_prov.json` temp file) → replaced with current project identity.
  - `grep -c aion scripts/provider-check.sh` now returns 0.

### Impact
- Files deleted: 0 (this session)
- Duplicate branches removed: 1 (`build_request` openai/cohere_v2)
- Dead probe lines removed: 3
- Stale branding references removed: 3 (`/tmp/aion_prov.json` rename counted once)

### Verification
- `python3 -m pytest -q` — 20 passed
- `ruff check .` — all checks passed
- `python3 -m compileall common.py probe_models.py generate_site.py` — clean
- `bash -n scripts/provider-check.sh` — syntax OK
- CI parity: matches `ci.yml` steps (`compileall` → `pytest -v` → `ruff check .`)

### Remaining (manual review needed)
- None blocking. `.cheahjs_snapshot` and the `jules-*`/`monitor-cheahjs.yml` bot
  workflows are operational automation with live references — kept.
- `generate_site.py` is upstream bot code by design (regenerated by
  `update-models.yml`) — never hand-edit, never lint.